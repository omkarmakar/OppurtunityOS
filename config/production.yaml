# Production environment overrides

environment: "production"
debug: false

database:
  echo: false
  pool_size: 20
  max_overflow: 30

logging:
  level: "INFO"
  rotation: "1 day"
  retention: "90 days"

server:
  host: "0.0.0.0"
  port: 8000
